training_target: 'rotation'
image_shape: [480, 640, 3] # H, W, C of the dataset images
input_shape: [225, 300, 1] # H, W, C of the desired input res for the network
epochs: 100
batch_size: 32
training_dataset_root: '/gate_pose_dataset/training/'
validation_dataset_root: '/gate_pose_dataset/validation'
alpha: 0.5
beta: 0.5
